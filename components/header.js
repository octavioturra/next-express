import React from 'react';

import styled from 'styled-components';

const Header = styled.header`
    width: 100%;
    height: 120px;
    background: linear-gradient(90deg, blue, navy);
`;

export default (props) => <Header></Header>;
